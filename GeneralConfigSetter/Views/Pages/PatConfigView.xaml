<Page x:Class="GeneralConfigSetter.Views.Pages.PatConfigView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GeneralConfigSetter.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="PatConfigView">

    <Grid Name="_patConfigGrid" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="16" Text="Pat Config Page" FontSize="18" HorizontalAlignment="Center"/>
        <ScrollViewer Grid.Row="1">
            <TextBox Text="{Binding PatConfig, UpdateSourceTrigger=PropertyChanged}" d:Text="Lorem Ipsum" TextWrapping="Wrap" AcceptsReturn="True"/>
        </ScrollViewer>

        <Button Grid.Row="2" HorizontalAlignment="Center" Margin="16" Content="Update PAT Config" Command="{Binding UpdatePatConfigCommand}"/>
    </Grid>
</Page>
